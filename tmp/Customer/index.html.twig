{% extends '::base.html.twig' %}
{% block body -%}
    <h1>Customer list</h1>

    <table class="records_list">
        <thead>
            <tr>
<!--
                <th>Id</th>
-->
                <th>Name</th>
                <th>Surname</th>
                <th>Pesel</th>
                <th>Email</th>
                <th>Phone</th>
                <td>Type</td>
<!--
                <th>Phone2</th>
                <th>Description</th>
                <th>Type</th>
                <th>Source</th>
                <th>Sourcetype</th>
                <th>Createdat</th>
-->
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
<!--
                <td><a href="{{ path('customer_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
-->
                <td><a href="{{ path('customer_show', { 'id': entity.id }) }}">{{ entity.name }}</a></td>
                <td>{{ entity.surname }}</td>
                <td>{{ entity.pesel }}</td>
                <td>{{ entity.email }}</td>
                <td>{{ entity.phone }}</td>
                <td>{{ entity.customerType }}</td>
<!--
                <td>{{ entity.phone2 }}</td>
                <td>{{ entity.description }}</td>
                <td>{{ entity.type }}</td>
                <td>{{ entity.source }}</td>
                <td>{{ entity.sourceType }}</td>
                <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
-->
                <td>
                <ul>
                    <li>
                        <a href="{{ path('customer_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('customer_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                    <li>
                        <a href="{{ path('event_customer', { 'id': entity.id }) }}">event</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('customer_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
